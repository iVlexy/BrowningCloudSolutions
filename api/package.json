{
  "name": "bcs-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute bcs-db --local --file=drizzle/0000_init.sql",
    "db:migrate:prod": "wrangler d1 execute bcs-db --file=drizzle/0000_init.sql",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.3",
    "hono": "^4.7.4",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "drizzle-kit": "^0.30.5",
    "typescript": "^5.7.3",
    "wrangler": "^4.88.0"
  }
}
